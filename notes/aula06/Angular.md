# CSS no Angular

* Os estilos em aplicações de Angular podem ser feitos de **duas maneiras**;
* **Global**: utilizado o arquivo *styles.css*, que fica nem src;
* **Scoped**: estilos a nível de componente, criamos quando damos um generate;